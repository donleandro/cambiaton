# Drizzle
DATABASE_URL=local.db

# Auth (opcional — si está vacío, la app es pública sin login)
# Generá ambos con: pnpm run hash-password "tu-password"
# Después copialos acá y reiniciá el dev server.
APP_PASSWORD_HASH=
SESSION_SECRET=
